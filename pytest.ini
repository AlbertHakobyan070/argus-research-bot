[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = src
markers =
    e2e: end-to-end Telegram bot tests; opt-in via ARGUS_E2E=1 (requires live bot token + FreeLLMAPI)
filterwarnings =
    ignore::DeprecationWarning